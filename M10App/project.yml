name: M10App
options:
  bundleIdPrefix: com.glmfunk
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  M10Kit:
    path: ../M10Kit

targets:
  M10App:
    type: application
    platform: iOS
    sources: [M10App]
    dependencies:
      - package: M10Kit
    info:
      path: M10App/Info.plist
      properties:
        CFBundleDisplayName: M10
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: >-
          M10 connects to your Leica camera's WiFi network to browse and
          download photos.
        NSBonjourServices: [_ptp._tcp]
        NSPhotoLibraryAddUsageDescription: >-
          M10 saves downloaded photos to your photo library, filing
          starred shots into “Best of Leica” and DNGs into “RAW Leica”.
        NSPhotoLibraryUsageDescription: >-
          M10 verifies saved photos and albums (read access only).
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        ITSAppUsesNonExemptEncryption: false
    settings:
      base:
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: NY422J2AAW
  M10AppTests:
    type: bundle.unit-test
    platform: iOS
    sources: [M10AppTests]
    dependencies:
      - target: M10App
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
  M10AppUITests:
    type: bundle.ui-testing
    platform: iOS
    sources: [M10AppUITests]
    dependencies:
      - target: M10App
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: M10App
schemes:
  M10App:
    build:
      targets: { M10App: all }
    test:
      targets: [M10AppTests, M10AppUITests]
